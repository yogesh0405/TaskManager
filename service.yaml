apiVersion: v1
kind: Service
metadata:
  name: taskmanager
  labels:
    app: taskmanager
spec:
  type: NodePort
  selector:
    app: taskmanager
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080